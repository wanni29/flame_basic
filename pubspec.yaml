name: flame_basic
description: "flame engine practice"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: 1.22.0 # 1.23버전은 불안정하므로 1.22로 진행

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/images/player.png
  - assets/images/apple.png